# Configuration

Environment-specific configuration belongs with each deployable application.

- Development: `.env.development.example`
- Production: `.env.production.example`

Copy the appropriate example to a local `.env` file when running the API directly. Secrets are never committed.
